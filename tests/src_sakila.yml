version: 2
  
sources:
  - name: sakila
    tables:
      - name: actor
        columns:
          - name: ACTOR_ID
            tests:
              - dbt_expectations.expect_column_values_to_be_unique